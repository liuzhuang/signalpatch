////////////////////////////////////////////////////
// 通过 Tailwind CSS 4 的 PostCSS 插件处理应用样式，不维护额外的转换插件链
////////////////////////////////////////////////////
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
